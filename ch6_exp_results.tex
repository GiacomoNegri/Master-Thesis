\chapter{Experiments and Results}
\label{ch:experiments_results}

\section{Introduction}
\label{sec:exp_introduction}
This chapter presents the experimental evaluation of the two modeling phases introduced
in Ch.~\ref{ch:theoretical_framework} and~\ref{ch:data_setup}, assessed with the
metrics defined in Ch.~\ref{ch:evaluation_methodology}. Two research questions guide
the analysis:

\begin{enumerate}
    \item \textbf{Phase~1 --- Replication.} Is a CSDI-based diffusion architecture capable
    of learning the distribution and reproducing the stylized facts of financial time series when trained
    under the SDE framework proposed by Kim et al.~\cite{kim2025diffusion}?
    \item \textbf{Phase~2 --- EDM-CSDI.} Does embedding the same architecture within the EDM framework of Karras et al.~\cite{karras2022elucidating}
    and conditioning generation on same-day Open, High, and Low log-returns improve
    generation quality over the Phase~1 baseline?
\end{enumerate}

The chapter is organised in four sequential parts. The former introduce two baseline models GARCH-X and Merton-X, this will function as baseline comparison for parametric models, widely accepted in the industry wide, understood and modeled. The second part will cover Phase~1 of this thesis concerning the replication of the work of Kim et al.~\cite{kim2025diffusion} on unconditional univariate generation of S\&P~500 close log-returns. The third phase, will investigate the contribution of the EDM framework when applied to Kim et al. CSDI based architectured, therefore it will retain unconditional information while introducing preconditioning in training. Lastly, the full EDM model will be presented incorporating conditional information and EDM framework.

Subsequently, others smaller experimentations were executed to compare the models behavior under different sampling procedures.

A key design difference that is worth to keep in mind between Phase~1 and Phase~2 concerns the validation split, as described in Sec.\ref{sec:sliding_window_ch4}. In Phase~1, validation windows are drawn randomly from the full sliding-window pool. In Phase~2, validation windows are strictly held out.

Moreover, because the way data are processed to build windows, the Reference values varies slightly according to the specific configuration that it is being analysed: raw reference, \(L=2048\) windows with \texttt{stride}\(=100\), \(L=512\) windows and \texttt{stride}\(=100\).

\section{Baseline Models --- GARCH-X and Merton-X}
GARCH-X and Merton-X models, defined in Sec.\ \ref{sec:classical_baselines_theoretical}, were trained on log transformed Sec.\ \ref{sec:ch4_data} data, according to Eq.\ \ref{eq:preprocessed_data}. Details about the design choices and constraints applied are available at App.\ \ref{app:baselines_constraints}.

The generated data display the following distributional statistics in Tab.\ \ref{tab:baseline_aggregate}; full statistics including all quantiles are reported in Tab.\ \ref{tab:baseline_full} of the appendix.

\begin{table}[htbp]
\centering
\footnotesize
\renewcommand{\arraystretch}{1.3}
\setlength{\tabcolsep}{4.5pt}
\begin{tabular}{
  l
  S[table-format= 1.4]   % Mean
  S[table-format= 1.4]   % Std
  S[table-format=-1.4]   % Skew.     (range: -7.5635 to 1.2102)
  S[table-format= 5.4]   % Ex.Kurt.  (up to 11021.7000)
  S[table-format=-1.4]   % q01
  S[table-format= 1.4]   % q99
  S[table-format= 1.4]   % D_KS
  S[table-format= 1.4]   % W_1
  S[table-format= 1.4]   % alpha
}
\toprule
\textbf{Model}
  & {\textbf{Mean}}
  & {\textbf{Std}}
  & {\textbf{Skew.}}
  & {\textbf{Ex.\ Kurt.}}
  & {$\boldsymbol{q_{0.01}}$}
  & {$\boldsymbol{q_{0.99}}$}
  & {$D_{\mathrm{KS}}$}
  & {$W_1$}
  & {$\hat{\alpha}$} \\
\midrule
Reference & 0.0005 & 0.0232 & -0.5773 &    38.5028 & -0.0626 & 0.0649 & {---} & {---} & \\
GARCH-X   & 0.0009 & 0.0230 & -7.5635 & 11021.7000 & -0.0577 & 0.0597 & 0.0540 & 0.0012 & \\
Merton-X  & 0.0005 & 0.0364 &  1.2102 &  3736.4870 & -0.0584 & 0.0598 & 0.0423 & 0.0018     & \\
\bottomrule
\end{tabular}
\caption{Aggregate distributional statistics for the GARCH-X and Merton-X baseline
models and the training reference. Values are computed over pooled log-returns. For Reference the entire dataset was used, while for GARCH-X, Merton-X a subsample of 10M observations was considered, while for \(D_{KS}\) and \(W_1\) a subsample of 1M was taken. Excess kurtosis is reported relative to the Gaussian.}
\label{tab:baseline_aggregate}
\end{table}

From the statistics it is possible to observe that this model, which has been refined within a working windows can match many of the distributional properties, see Tab.\ \ref{tab:baseline_full}, but they still struggle with higher moments, mostly because even if bound, the generation still tends to drift afar. Moreover, if we have a look at Fig.\ [STYLIZED FACTS COMBINED], we can see that the key properties of fat tails distribution and volatility clustering could are or could possibly be learned, while leverage effects are clearly missed. Moreover these models were fitted at a stock level, therefore their parameters are meaningless for different models.




\section{Phase 1 --- Replication}
\label{sec:phase1_results}
Phase~1 aims to replicate the generative approach of Kim et al.~\cite{kim2025diffusion} as detailed in the first column of Tab.\ \ref{tab:training_setup}. For each model a dataset of 512 windows of length \(L=2048\) were generated using probability-flow ODE, Eq.\ref{eq:probability_flow} with 2000 discretization steps. WHY 512 windows and not more

[RIPRENDERE DA QUI]

\textbf{Aggregate Statistics and Distributional Similarity} \quad 

% Required packages: booktabs, array, siunitx
% Add to preamble if not present:
%   \usepackage{siunitx}
%   \sisetup{detect-weight=true, detect-inline-weight=math}
%
% Corrections applied vs. previous version:
%   1. VE+Linear    W1:          0.010 → 0.007
%   2. VP+Linear    std:         0.0446 → 0.045
%   3. VP+Exponential mean:     -0.001 → 0.001  (sign error)
%   4. VP+Cosine    W1:          0.013 → 0.003
%   5. Ex.\ Kurt. column format: S[2.3] → S[3.3]  (529.825 needs 3 integer digits)
%   6. q01 column format:        S[2.3] → S[-1.4] (4 decimal places needed)
%   7. Bolding: alpha moved from VE+Exp (4.461) to VP+Cosine (4.368),
%      which is closest to reference 4.378; |4.368-4.378|=0.010 vs |4.461-4.378|=0.083

\begin{table}[htbp]
\centering
\footnotesize
\renewcommand{\arraystretch}{1.3}
\setlength{\tabcolsep}{4.5pt}
\begin{tabular}{
  l
  S[table-format= 1.4]   % Mean:     ±0.xxxx
  S[table-format= 1.5]   % Std:      0.xxxx
  S[table-format= 1.4]   % Skew.:    x.xxxx  (handles 6.3270)
  S[table-format= 3.3]   % Ex.Kurt.: xxx.xxx (handles 529.825)
  S[table-format=-1.4]   % q01:     -x.xxxx
  S[table-format= 1.4]   % q99:      x.xxxx
  S[table-format= 1.3]   % KS:       x.xxx
  S[table-format= 1.3]   % W1:       x.xxx
  S[table-format= 1.3]   % alpha:    x.xxx
}
\toprule
\textbf{Configuration}
  & {\textbf{Mean}}
  & {\textbf{Std}}
  & {\textbf{Skew.}}
  & {\textbf{Ex.\ Kurt.}}
  & {$\boldsymbol{q_{0.01}}$}
  & {$\boldsymbol{q_{0.99}}$}
  & {$D_{\mathrm{KS}}$}
  & {$W_1$}
  & {$\hat{\alpha}$} \\
\midrule
% Training reference (no KS/W1 — computed against itself; alpha from empirical data)
Reference        & 0.0005 & 0.0208 & -0.5387 &  36.063 & -0.0562 & 0.0572 & {---} & {---} & 4.378 \\
\midrule
% VE family
VE + Linear      &  0.0004 & 0.0270 & -0.1712 &   7.052 & -0.0649 & 0.0659 & 0.121 & 0.007 & 5.429 \\
VE + Exponential &  0.0005 & 0.0190 & -0.0860 &  14.343 & -0.0479 & 0.0492 & \bfseries 0.037 & \bfseries 0.002 & 4.461 \\
VE + Cosine      &  0.0008 & 0.0221 & -0.5979 &  21.459 & -0.0558 & 0.0575 & 0.062 & 0.003 & 4.448 \\
\midrule
% VP family
VP + Linear      &  0.0023 & 0.0446 &  6.3270 & 529.825 & -0.0739 & 0.0891 & 0.142 & 0.011 & 4.883 \\
VP + Exponential &  0.0011 & 0.0546 &  0.1530 &   5.626 & -0.1365 & 0.1539 & 0.206 & 0.025 & 5.535 \\
VP + Cosine      &  0.0003 & 0.0235 & -0.2985 &  15.799 & -0.0607 & 0.0627 & 0.060 & 0.003 & \bfseries 4.368 \\
\midrule
% GBM family (no alpha — distributions too far from training for power-law fitting)
GBM + Linear     &  0.0001 & 0.2797 & -0.0023 &   0.018 & -0.6519 & 0.6516 & 0.419 & 0.210 & 13.055 \\
GBM + Exponential&  0.0001 & 0.1453 & -0.0122 &   0.073 & -0.3385 & 0.3379 & 0.370 & 0.101 & {10.935} \\
GBM + Cosine     &  0.0000 & 0.1437 & -0.0130 &   0.068 & -0.3350 & 0.3342 & 0.371 & 0.102 & {11.134} \\
\bottomrule
\end{tabular}
\caption{Aggregate distributional statistics for Phase~1 generated close
log-returns and the training reference. Values are computed over pooled
window-level \((L=2048, \texttt{ stride}=100)\) returns. Excess kurtosis is
reported relative to the Gaussian. $D_{\mathrm{KS}}$ and $W_1$
measure discrepancy against the training marginal (lower is better).
$\hat{\alpha}$ is the estimated power-law tail exponent (reference
$\hat{\alpha}=4.378$; closer is better).}
\label{tab:phase1_aggregate}
\end{table}

%%%%%%%%%%%%%%%%%%%%%%%
[WRITE INTERPRETATION]
The statistics above are computed on \(n=1,048,576\) for each trained configuration.


% [PLACEHOLDER: TABLE — aggregate distributional statistics (mean, std, skewness, excess
% kurtosis, min, max) for generated vs. training close log-returns across all 7 Phase 1
% configurations. Rows: training reference, VE-linear, VE-exponential, VE-cosine,
% VP-linear, GBM-linear, GBM-exponential, GBM-cosine. Bold best per column.]

% [PLACEHOLDER: FIGURE — QQ plots (top row) and ECDF comparisons (bottom row) for
% a representative subset of Phase 1 configurations, e.g., one per SDE family (VE-best,
% VP-linear, GBM-best) and the training reference. KS gap marked on each ECDF panel.]
\begin{figure}[htbp]
    \centering
    % [PLACEHOLDER: figure content]
    \caption{Quantile--quantile plots (top row) and empirical cumulative distribution
    function comparisons (bottom row) for selected Phase~1 configurations. Generated
    distributions are shown as dashed lines; the training reference as a solid line.
    The maximum vertical gap in each ECDF panel corresponds to the KS statistic
    $D_{\text{KS}}$.}
    \label{fig:phase1_qq_ecdf}
\end{figure}

Clearly the Exponental and cosine seem to be superior, as well as the ve sde.

Based on the results in Tab.~\ref{tab:phase1_aggregate} and
Fig.~\ref{fig:phase1_qq_ecdf},
% [PLACEHOLDER: narrative interpretation — which configurations achieve the lowest KS
% and W1 values? Are skewness and excess kurtosis reproduced by any SDE variant?
% Does the GBM-inspired SDE or any particular noise schedule show a consistent
% advantage in distributional matching? Note whether the bulk or the tails are the
% main source of discrepancy across configurations.]

\textbf{Stylized Facts}
Heavy tails \(\alpha\) coefficient was estimated on a subsample of \(300,000\) observations using \texttt{powerlaw} package from Alstott et al.\ \cite{alstott2014powerlaw}

Our estimation of the \(\alpha\) component is in line with Kim el al., which reported a \(\alpha\approx4.35\), while our estimation is \(4.37\), therefore in line. An estimation below this benchmark indicates that the data has a heavier tails.

The reference stylized facts are display here and have been recomputed from Sec.\ \ref{sec:fts_literature} on the windows, in order to guranteed a fair comparison with generated stylized facts.

% Required packages (add to preamble if not already present):
%   \usepackage{graphicx}
%   \usepackage{subcaption}
%   \usepackage{rotating}    % for \rotatebox
%
% HOW IT WORKS
% ------------
% Column headers: a row of three \minipage blocks, offset by \hspace{1.8em}
%   to align with the subfigure columns beneath (the offset matches the
%   width consumed by the \rotatebox row label).
% Row labels: \rotatebox[origin=c]{90}{...} placed immediately before
%   the first subfigure of each row, with a small \hspace{0.4em} gap.
% Panel widths: each subfigure is 0.30\textwidth, with \hfill between them.
%   Adjust to 0.29\textwidth if images have wide whitespace margins.
% Subcaption labels are suppressed (no \caption inside subfigure) so only
%   the single figure-level \caption appears.
% Cross-reference individual panels via their \label if needed.

\begin{figure}[htbp]
\centering

% ----------------------------------------------------------------
% COLUMN HEADERS
% ----------------------------------------------------------------
\hspace{1.8em}%
\begin{minipage}[c]{0.30\textwidth}
  \centering\small\textbf{Heavy-tail distribution}
\end{minipage}%
\hfill
\begin{minipage}[c]{0.30\textwidth}
  \centering\small\textbf{Volatility clustering}
\end{minipage}%
\hfill
\begin{minipage}[c]{0.30\textwidth}
  \centering\small\textbf{Leverage effect}
\end{minipage}

\vspace{0.4em}
{\color{black!25}\hrule}
\vspace{0.2em}

% ----------------------------------------------------------------
% ROW 0: Empirical reference      [BUG 1 FIXED: single \rotatebox]
% ----------------------------------------------------------------
\rotatebox[origin=c]{90}{\small\textbf{Reference}}\hspace{0.4em}%
\begin{subfigure}[c]{0.30\textwidth}
  \centering
  \includegraphics[width=\linewidth]{images/Phase1/pos_distribution_reference_2048.png}
  \label{fig:ref_tail}
\end{subfigure}\hfill
\begin{subfigure}[c]{0.30\textwidth}
  \centering
  \includegraphics[width=\linewidth]{images/Phase1/volatility_clustering_refernce_2048.png}
  \label{fig:ref_vol}
\end{subfigure}\hfill
\begin{subfigure}[c]{0.30\textwidth}
  \centering
  \includegraphics[width=\linewidth]{images/Phase1/leverage_effects_reference_2048.png}
  \label{fig:ref_lev}
\end{subfigure}%                   no trailing \hfill, % suppresses whitespace

\vspace{0.15em}

% ----------------------------------------------------------------
% ROW 1: VE SDE
% ----------------------------------------------------------------
\rotatebox[origin=c]{90}{\small\textbf{VE + Exp.}}\hspace{0.4em}%
\begin{subfigure}[c]{0.30\textwidth}
  \centering
  \includegraphics[width=\linewidth]{images/Phase1/pos_distribution_ve-exp_2048.png}
  \label{fig:ve_tail}
\end{subfigure}\hfill
\begin{subfigure}[c]{0.30\textwidth}
  \centering
  \includegraphics[width=\linewidth]{images/Phase1/volatility_clustering_ve-exp_2048.png}
  \label{fig:ve_vol}
\end{subfigure}\hfill
\begin{subfigure}[c]{0.30\textwidth}
  \centering
  \includegraphics[width=\linewidth]{images/Phase1/leverage_effects_ve-exp_2048.png}
  \label{fig:ve_lev}
\end{subfigure}%                   [BUG 2 FIXED: % instead of blank line]

\vspace{0.2em}

% ----------------------------------------------------------------
% ROW 2: VP SDE
% ----------------------------------------------------------------
\rotatebox[origin=c]{90}{\small\textbf{VP + Cos.}}\hspace{0.4em}%
\begin{subfigure}[c]{0.30\textwidth}
  \centering
  \includegraphics[width=\linewidth]{images/Phase1/pos_distribution_vp-cos_2048.png}
  \label{fig:vp_tail}
\end{subfigure}\hfill
\begin{subfigure}[c]{0.30\textwidth}
  \centering
  \includegraphics[width=\linewidth]{images/Phase1/volatility_clustering_vp-cos_2048.png}
  \label{fig:vp_vol}
\end{subfigure}\hfill
\begin{subfigure}[c]{0.30\textwidth}
  \centering
  \includegraphics[width=\linewidth]{images/Phase1/leverage_effects_vp-cos_2048.png}
  \label{fig:vp_lev}
\end{subfigure}%

\vspace{0.2em}

% ----------------------------------------------------------------
% ROW 3: GBM SDE
% ----------------------------------------------------------------
\rotatebox[origin=c]{90}{\small\textbf{GBM + Exp.}}\hspace{0.4em}%
\begin{subfigure}[c]{0.30\textwidth}
  \centering
  \includegraphics[width=\linewidth]{images/Phase1/pos_distribution_gbm-exp_2048.png}
  \label{fig:gbm_tail}
\end{subfigure}\hfill
\begin{subfigure}[c]{0.30\textwidth}
  \centering
  \includegraphics[width=\linewidth]{images/Phase1/volatility_clustering_gbm-exp_2048.png}
  \label{fig:gbm_vol}
\end{subfigure}\hfill
\begin{subfigure}[c]{0.30\textwidth}
  \centering
  \includegraphics[width=\linewidth]{images/Phase1/leverage_effects_gbm-exp_2048.png}
  \label{fig:gbm_lev}
\end{subfigure}%

\vspace{0.2em}

\caption{Stylized facts across Phase~1 SDE families and the empirical training
reference on windows with \(L=2048\). \textit{Columns} (left to right): heavy-tail distribution on a log--log
scale with estimated power-law exponent~$\hat{\alpha}$, Tab.\ref{tab:phase1_aggregate}; autocorrelation of absolute
returns on a log--log scale (volatility clustering); lead--lag correlation $L(k)$
(leverage effect).}
\label{fig:phase1_stylized_grid}
\end{figure}

Based on Fig.~\ref{fig:phase1_leverage},
% [PLACEHOLDER: narrative — do any configurations reproduce persistent negative L(k)
% for small positive lags? Is the GBM-inspired SDE notably better than VE or VP?
% If no configuration captures the leverage effect, note this as a structural
% limitation: a Gaussian diffusion kernel, even with heteroskedastic inspiration,
% may lack the directional asymmetry required to generate this property.]

\textbf{Sample Paths}


\begin{figure}[htbp]
    \centering
    % [PLACEHOLDER: figure content]
    \caption{Unnormalised price paths generated by the best Phase~1 configuration
    (left panel, multiple samples shown in light colour with mean trajectory overlaid)
    and representative real S\&P~500 price trajectories (right panel) over a comparable
    time window. Both panels are normalised to a common starting price of 100 for
    visual comparison.}
    \label{fig:phase1_paths}
\end{figure}

% [PLACEHOLDER: narrative — brief qualitative assessment of whether generated paths
% exhibit plausible drift, volatility clustering, and occasional sharp movements.
% Note any obvious structural deviations from realistic equity price behaviour, such
% as excessive mean-reversion, lack of regime changes, or path crossings that would
% violate non-negativity of prices at realistic price levels.]

\textbf{Summary and Selection of Best Phase~1 Configuration}

In conclusion, it seem that under certain condition, it could work. but we discard GBM approach

% [PLACEHOLDER: TABLE — summary ranking of all 7 Phase 1 configurations. Rows:
% VE-linear, VE-exponential, VE-cosine, VP-linear, GBM-linear, GBM-exponential,
% GBM-cosine. Columns: KS, W1, alpha, ACF decay quality (qualitative: good/moderate/poor),
% leverage effect (qualitative), overall rank. Highlight (bold) the selected
% best configuration.]
\begin{table}[htbp]
    \centering
    \caption{Summary of Phase~1 configuration performance across primary evaluation
    metrics. The tail exponent $\alpha$ should be compared against the empirical
    benchmark of approximately $4.35$. Volatility clustering and leverage effect are
    assessed qualitatively from the corresponding figures. The configuration selected
    as the Phase~1 baseline for Phase~2 comparison is highlighted in bold.}
    \label{tab:phase1_ranking}
    % [PLACEHOLDER: table content]
\end{table}

\section{Phase 2 --- EDM-CSDI Conditional Generation}
\label{sec:phase2}

\begin{itemize}
    \item Introduction
    \item Baseline model
    \item comparison with equivalent Phase1
    \item comparison with GARCH and Merton (industry)
    \item Unconditional comparison
    \item Stochastic sampler
    \item variations
    \item (optional) Post-2002
\end{itemize}

Phase~2 extends the explored CSDI-backbone architecture to the multivariate conditional
setting within the EDM framework. Relative to Phase~1, three structural changes are
introduced, each with a distinct theoretical motivation.First, the EDM training framework with preconditioning and structured presented in previous chapters. Second, Multivariete OHLC conditioning. Thirdly, the validation windows are now fully held out and sampling windows' length is reduced to \(L=512\) and \(s=100\).

Unless otherwise stated, in this section we use "EDM sampler" for Heun's ODE second order approximation with 200 discretization steps (400 NFE) and \(\sigma_{\max}=P_{\text{mean}}+5P_{\text{std}}\).

\textbf{EDM Baseline Model} \quad After a series of ablations and experimentations the baseline models has the characteristics reported in Tab.\ \ref{tab:training_setup}, of which the most important are \(P_{\text{mean}}=-1.4\) and \(P_{\text{std}}=1.8\).

In order to assess the improvements brought by the implementation of EDM framework with the CSDI backbone and conditioning, the next phase was structured in three steps: first we compare with interpretable industry wide accepted models, such as GARCH-X and Merton-X, this will give us a performance baseline for comparison. Secondly, the Baseline will be compared against the best models of Phase~1 trained with \(L=512\) and \(s=100\), similarly to EDM. This will enable us to, understand Kim et al.\ \cite{kim2025diffusion} improvement on classical modeling and most importantly the oThirdly, it would be compared against an unconditional v

\textbf{Phase~1 Champions} \quad In order to understand the improvements brough
%%%%%%%%%%%%%%%%%%%%%%%

A necessary condition for attributing Phase~2 improvements to the EDM framework or to
the conditioning signal is a fair comparison with the Phase~1 baseline on identical
windows and identical evaluation splits. To this end, the best Phase~1 configuration
identified in Sec.~\ref{sec:phase1_summary} is retrained from scratch at $L=512$ and
stride $s=100$, using the probability flow ODE sampler and the same validation split
employed in Phase~2. This retrained model is referred to as Phase~1$^*$ throughout
this section. Because the window length, stride, and evaluation split are now identical
for both models, differences in generation quality can be attributed to the modelling
choices rather than to differences in the data seen during training.

The comparison is conducted on the metrics used throughout Phase~1 --- KS statistic,
$W_1$ distance, tail exponent $\alpha$, and pathwise aggregate statistics --- computed
on the generated Close log-return channel in both cases. CRPS is reported for Phase~2 BM
but is not applicable to Phase~1$^*$, which is unconditional and does not produce a
predictive distribution over the Close channel given OHL context. The isolated
contribution of conditioning to generation quality is examined in
Sec.~\ref{sec:phase2_unconditional}, which introduces an unconditional EDM baseline
as an intermediate control.
