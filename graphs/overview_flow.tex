\begin{figure}[h]
    \centering

\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
    font=\small,
    >=Latex,
    node distance=0.7cm and 1.0cm,
    inputblock/.style={
        draw=black,
        fill=red!12,
        rounded corners=2pt,
        minimum width=2.8cm,
        minimum height=0.9cm,
        align=center
    },
    convblock/.style={
        draw=blue!70!black,
        fill=blue!15,
        rounded corners=2pt,
        minimum width=4.0cm,
        minimum height=1.0cm,
        align=center,
        thick
    },
    resblock/.style={
        draw=black!70,
        fill=gray!15,
        rounded corners=3pt,
        minimum width=4.3cm,
        minimum height=1.3cm,
        align=center,
        thick
    },
    opblock/.style={
        draw=black!70,
        fill=yellow!20,
        rounded corners=2pt,
        minimum width=3.5cm,
        minimum height=0.8cm,
        align=center
    },
    skipblock/.style={
        draw=black!70,
        fill=green!12,
        rounded corners=2pt,
        minimum width=3.5cm,
        minimum height=0.8cm,
        align=center
    },
    shapetext/.style={
        font=\scriptsize,
        align=center
    },
    mainarrow/.style={->, thick},
    skiparrow/.style={->, thick, dashed},
    projblock/.style={
        draw=purple!70!black,
        fill=purple!12,
        rounded corners=2pt,
        minimum width=3.7cm,
        minimum height=0.8cm,
        align=center
    },
    sidearrow/.style={->, thick, dashed}
]

% ============================================================
% MAIN ROW -- left-to-right data flow
% ============================================================

\node[inputblock] (input) {
    \textbf{Input}
};

% Moved slightly left by reducing distance from Input
\node[convblock, right=0.55cm of input] (inputproj) {
    \textbf{input\_projection}\\
    Conv1d, kernel size $1$
};

% Increased distance from input_projection so ResidualBlock remains visually separated
\node[resblock, right=2.75cm of inputproj] (residual) {
    \textbf{ResidualBlock}\\
    with skip taps
};

% Moved skip aggregation slightly to the right
\node[skipblock, right=1.75cm of residual] (sumskip) {
    $\displaystyle \sum \mathrm{skips}/\sqrt{n}$
};

\node[convblock, right=0.9cm of sumskip] (outputproj) {
    \textbf{output\_projection}\\
    Conv1d, kernel size $1$
};

\node[opblock, right=0.9cm of outputproj] (reshape) {
    \textbf{Reshape}
};

\node[inputblock, right=0.9cm of reshape] (output) {
    \textbf{Output}
};

% ============================================================
% DIMENSION LABELS -- upper-right corner of every block
% ============================================================

\node[shapetext, anchor=south west]
    at ($(input.north east)+(-1,0.05)$)
    {$(B,2,K,L)$};

\node[shapetext, anchor=south west]
    at ($(inputproj.north east)+(-1.0,0.05)$)
    {$(B,\mathrm{ch},K,L)$};

\node[shapetext, anchor=south west]
    at ($(residual.north east)+(0.10,0.05)$)
    {$(B,\mathrm{ch},K{\times}L)$};

\node[shapetext, anchor=south west]
    at ($(sumskip.north east)+(-1.0,0.05)$)
    {$(B,\mathrm{ch},K{\times}L)$};

\node[shapetext, anchor=south west]
    at ($(outputproj.north east)+(-1.0,0.05)$)
    {$(B,1,K{\times}L)$};

\node[shapetext, anchor=south west]
    at ($(reshape.north east)+(-1.0,0.05)$)
    {$(B,K,L)$};

\node[shapetext, anchor=south west]
    at ($(output.north east)+(-1.0,0.05)$)
    {$(B,K,L)$};

% ============================================================
% MAIN FLOW ARROWS -- left to right
% ============================================================

\draw[mainarrow] (input)      -- (inputproj);
\draw[mainarrow] (inputproj)  -- (residual);
\draw[mainarrow] (residual)   -- (sumskip);
\draw[mainarrow] (sumskip)    -- (outputproj);
\draw[mainarrow] (outputproj) -- (reshape);
\draw[mainarrow] (reshape)    -- (output);

% ============================================================
% CONDITIONING BLOCKS around ResidualBlock
% ============================================================

% Diffusion conditioning branch
\node[projblock] (diffproj)
    at ($(residual)+(-2.9cm,-2.0cm)$)
{
    \textbf{diffusion\_projection}\\
    Linear $(\mathrm{demb\_dim}\to \mathrm{ch})$
};

\node[inputblock] (cnoise)
    at ($(residual)+(-2.9cm,-3.45cm)$)
{
    \textbf{$c_{\mathrm{noise}}$}
};

% Conditional side information branch
\node[projblock] (condproj)
    at ($(residual)+(+2.9cm,-2.0cm)$)
{
    \textbf{cond\_projection}\\
    Conv1d $(193\to2\mathrm{ch})$, $k{=}1$
};

\node[inputblock] (sideinfo)
    at ($(residual)+(+2.9cm,-3.45cm)$)
{
    \textbf{side\_info}
};

% Sinusoidal positional encoding branch
\node[inputblock] (sinpe)
    at ($(residual)+(0cm,2.25cm)$)
{
    \textbf{Sinusoidal PE}
};

% ============================================================
% DIMENSION LABELS for conditioning blocks
% ============================================================

\node[shapetext, anchor=south west]
    at ($(cnoise.north east)+(0.0,-0.2)$)
    {$(B,\mathrm{demb\_dim})$};

\node[shapetext, anchor=south west]
    at ($(diffproj.north east)+(-1,-0.1)$)
    {$(B,\mathrm{ch},K{\times}L)$};

\node[shapetext, anchor=south west]
    at ($(sideinfo.north east)+(0.0,-0.2)$)
    {$(B,\mathrm{d_{temb}}+\mathrm{d_{femb}}+1)$};

\node[shapetext, anchor=south west]
    at ($(condproj.north east)+(-1,-0.1)$)
    {$(B,2\mathrm{ch},K{\times}L)$};

\node[shapetext, anchor=south west]
    at ($(sinpe.north east)+(0.0,-0.2)$)
    {$(B,\mathrm{ch},L)$};

% ============================================================
% CONDITIONING ARROWS
% ============================================================

% c_noise -> diffusion_projection
\draw[sidearrow] (cnoise) -- (diffproj);

% diffusion_projection -> ResidualBlock
\draw[sidearrow] (diffproj.north) -- (residual.south west);

% side_info -> cond_projection
\draw[sidearrow] (sideinfo) -- (condproj);

% cond_projection -> ResidualBlock
\draw[sidearrow] (condproj.north) -- (residual.south east);

% Sinusoidal PE -> ResidualBlock
\draw[sidearrow] (sinpe.south) -- (residual.north);

% ============================================================
% SKIP-TAP ILLUSTRATION
% Exits ResidualBlock, goes above the main row, enters sumskip.
% ============================================================

\coordinate (skipstart) at (residual.north east);
\coordinate (skipup)    at ($(skipstart)+(0,1.05cm)$);
\coordinate (skipright) at (skipup -| sumskip.north);

\draw[skiparrow]
    (skipstart)
    -- (skipup)
    -- node[midway, above=0.10cm, font=\scriptsize, align=center]
          {skip outputs}
    (skipright)
    -- (sumskip.north);

% ============================================================
% REPEATED-BLOCK ANNOTATION
% ============================================================

\node[anchor=west, font=\large]
    at ($(residual.east)+(0.30cm,0.45cm)$)
    {$\times n$};

% ============================================================
% DASHED CONTAINER around only the repeated ResidualBlock
% Embeddings stay outside the repeated unit.
% ============================================================

\node[
    draw=black,
    dashed,
    rounded corners=4pt,
    inner sep=0.18cm,
    fit=(residual)
] (rescontainer) {};

\end{tikzpicture}
} % end resizebox

    \caption{High-level overview of the CSDI denoising network. The two-channel input is projected into a latent space, processed by $n$ stacked residual blocks with skip connections, and decoded back to the predicted clean signal.}
    \label{fig:csdi_overview}
\end{figure}